# ################################
# Model: DFM v3 + CTC ASR on TIMIT with CRDNN
# Hybrid objective:
#   features -> encoder -> logits -> DFM -> log-probs -> CTC
# plus explicit flow-matching loss on a Dirichlet bridge
# ################################

seed: 1986
run_name: dfm_v3
output_folder: !ref results/dfm_tests/<run_name>
__set_seed: !apply:speechbrain.utils.seed_everything [!ref <seed>]
test_wer_file: !ref <output_folder>/wer_test.txt
save_folder: !ref <output_folder>/save
train_log: !ref <output_folder>/train_log.txt

# ----------------------------------------------------------------------
# Data files
# ----------------------------------------------------------------------
data_folder: "E:/dfm_asr/data/TIMIT/data"
train_annotation: !ref <save_folder>/train.json
valid_annotation: !ref <save_folder>/dev.json
test_annotation: !ref <save_folder>/test.json

# New run_name => new save folder, so keep this False for the first run.
skip_prep: False
uppercase: True

# ----------------------------------------------------------------------
# Data for augmentation
# ----------------------------------------------------------------------
data_folder_noise: !ref <data_folder>/noise
NOISE_DATASET_URL: https://www.dropbox.com/scl/fi/a09pj97s5ifan81dqhi4n/noises.zip?rlkey=j8b0n9kdjdr32o1f06t0cw5b7&dl=1
noise_annotation: !ref <save_folder>/noise.csv

# ----------------------------------------------------------------------
# Training parameters
# ----------------------------------------------------------------------
number_of_epochs: 40
batch_size: 16
lr: 1.0
sorting: ascending

# ----------------------------------------------------------------------
# Feature parameters
# ----------------------------------------------------------------------
sample_rate: 16000
n_fft: 400
n_mels: 40

# ----------------------------------------------------------------------
# Baseline model parameters
# ----------------------------------------------------------------------
activation: !name:torch.nn.LeakyReLU
dropout: 0.15
cnn_blocks: 2
cnn_channels: (128, 256)
cnn_kernelsize: (3, 3)
rnn_layers: 4
rnn_neurons: 512
rnn_bidirectional: True
dnn_blocks: 2
dnn_neurons: 512

# ----------------------------------------------------------------------
# Outputs
# ----------------------------------------------------------------------
output_neurons: 40
blank_index: 0

# ----------------------------------------------------------------------
# DFM parameters
# ----------------------------------------------------------------------
dfm_hidden: 256
dfm_num_flow_steps: 4
dfm_epsilon: 1.0e-6

# Dirichlet bridge parameters
dirichlet_target_scale: 20.0
dirichlet_bridge_temperature: 1.0

# ----------------------------------------------------------------------
# Hybrid loss parameters
# ----------------------------------------------------------------------
target_smoothing: 0.02
lambda_ctc: 1.0
lambda_fm: 0.5
lambda_reg: 0.05

# ----------------------------------------------------------------------
# Dataloader options
# ----------------------------------------------------------------------
num_workers: 0

train_dataloader_opts:
    batch_size: !ref <batch_size>
    num_workers: !ref <num_workers>

valid_dataloader_opts:
    batch_size: !ref <batch_size>
    num_workers: !ref <num_workers>

test_dataloader_opts:
    batch_size: !ref <batch_size>
    num_workers: !ref <num_workers>

# ----------------------------------------------------------------------
# Normalization
# ----------------------------------------------------------------------
normalize: !new:speechbrain.processing.features.InputNormalization
    norm_type: global

# ----------------------------------------------------------------------
# Augmentations
# ----------------------------------------------------------------------
prepare_noise_data: !name:speechbrain.augment.preparation.prepare_dataset_from_URL
    URL: !ref <NOISE_DATASET_URL>
    dest_folder: !ref <data_folder_noise>
    ext: wav
    csv_file: !ref <noise_annotation>

add_noise: !new:speechbrain.augment.time_domain.AddNoise
    csv_file: !ref <noise_annotation>
    snr_low: 5
    snr_high: 15
    noise_sample_rate: !ref <sample_rate>
    clean_sample_rate: !ref <sample_rate>
    num_workers: !ref <num_workers>

speed_perturb: !new:speechbrain.augment.time_domain.SpeedPerturb
    orig_freq: !ref <sample_rate>
    speeds: [98, 100, 102]

drop_freq: !new:speechbrain.augment.time_domain.DropFreq
    drop_freq_low: 0
    drop_freq_high: 1
    drop_freq_count_low: 1
    drop_freq_count_high: 2
    drop_freq_width: 0.03

drop_chunk: !new:speechbrain.augment.time_domain.DropChunk
    drop_length_low: 400
    drop_length_high: 1000
    drop_count_low: 1
    drop_count_high: 2

wav_augment: !new:speechbrain.augment.augmenter.Augmenter
    concat_original: False
    min_augmentations: 1
    max_augmentations: 2
    augment_prob: 0.5
    augmentations: [
        !ref <add_noise>,
        !ref <speed_perturb>,
        !ref <drop_freq>,
        !ref <drop_chunk>
    ]

# ----------------------------------------------------------------------
# Training helpers
# ----------------------------------------------------------------------
epoch_counter: !new:speechbrain.utils.epoch_loop.EpochCounter
    limit: !ref <number_of_epochs>

compute_features: !new:speechbrain.lobes.features.Fbank
    sample_rate: !ref <sample_rate>
    n_fft: !ref <n_fft>
    n_mels: !ref <n_mels>

# ----------------------------------------------------------------------
# Encoder / acoustic model
# ----------------------------------------------------------------------
model: !new:speechbrain.lobes.models.CRDNN.CRDNN
    input_size: !ref <n_mels>
    activation: !ref <activation>
    dropout: !ref <dropout>
    cnn_blocks: !ref <cnn_blocks>
    cnn_channels: !ref <cnn_channels>
    cnn_kernelsize: !ref <cnn_kernelsize>
    time_pooling: True
    rnn_layers: !ref <rnn_layers>
    rnn_neurons: !ref <rnn_neurons>
    rnn_bidirectional: !ref <rnn_bidirectional>
    dnn_blocks: !ref <dnn_blocks>
    dnn_neurons: !ref <dnn_neurons>

output: !new:speechbrain.nnet.linear.Linear
    input_size: !ref <dnn_neurons>
    n_neurons: !ref <output_neurons>
    bias: True

# ----------------------------------------------------------------------
# DFM block
# ----------------------------------------------------------------------
dfm: !new:__main__.DFMModule
    vocab_size: !ref <output_neurons>
    hidden_size: !ref <dnn_neurons>
    dfm_hidden: !ref <dfm_hidden>
    num_flow_steps: !ref <dfm_num_flow_steps>
    epsilon: !ref <dfm_epsilon>
    target_scale: !ref <dirichlet_target_scale>
    bridge_temperature: !ref <dirichlet_bridge_temperature>

log_softmax: !new:speechbrain.nnet.activations.Softmax
    apply_log: True

# ----------------------------------------------------------------------
# Optimizer / scheduler
# ----------------------------------------------------------------------
opt_class: !name:torch.optim.Adadelta
    rho: 0.95
    lr: !ref <lr>
    eps: 1.e-8

lr_annealing: !new:speechbrain.nnet.schedulers.NewBobScheduler
    initial_value: !ref <lr>
    improvement_threshold: 0.0025
    annealing_factor: 0.8
    patient: 0

# ----------------------------------------------------------------------
# Registered trainable modules
# ----------------------------------------------------------------------
modules:
    model: !ref <model>
    output: !ref <output>
    dfm: !ref <dfm>
    normalize: !ref <normalize>

jit_module_keys: [model]

# ----------------------------------------------------------------------
# Checkpointing
# ----------------------------------------------------------------------
checkpointer: !new:speechbrain.utils.checkpoints.Checkpointer
    checkpoints_dir: !ref <save_folder>
    recoverables:
        model: !ref <model>
        output: !ref <output>
        dfm: !ref <dfm>
        scheduler: !ref <lr_annealing>
        normalizer: !ref <normalize>
        counter: !ref <epoch_counter>

# ----------------------------------------------------------------------
# Loss / logging / metrics
# ----------------------------------------------------------------------
compute_cost: !name:speechbrain.nnet.losses.ctc_loss
    blank_index: !ref <blank_index>

train_logger: !new:speechbrain.utils.train_logger.FileTrainLogger
    save_file: !ref <train_log>

ctc_stats: !name:speechbrain.utils.metric_stats.MetricStats
    metric: !name:speechbrain.nnet.losses.ctc_loss
        blank_index: !ref <blank_index>
        reduction: batch

per_stats: !name:speechbrain.utils.metric_stats.ErrorRateStats