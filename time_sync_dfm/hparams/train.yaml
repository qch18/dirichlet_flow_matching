# ################################
# Stage 13: Direction-Magnitude Decoupled Time-Synchronous DFM
#
# Three fixes for the velocity magnitude overfitting problem:
#
# Fix 1 (inference): Predicted velocities are normalised to unit length
#   then scaled by velocity_inference_scale. Direction from the learned
#   net, magnitude from a fixed parameter. No overfitting possible.
#
# Fix 2 (architecture): Back to 512 hidden, 0.1 dropout, plus L2 weight
#   decay on velocity net. Keeps 3 conv layers and GroupNorm from Stage 12.
#   Less capacity = less memorisation of training-specific patterns.
#
# Fix 3 (training): Target velocities normalised to unit length. The
#   velocity net only learns direction, not magnitude. All segments have
#   L2=1.0 targets regardless of phone duration, eliminating the variance
#   that caused magnitude overfitting.
#
# lambda_fm is 1.0 (was 0.02) because normalised targets give FM loss
# on the order of 0.5-2.0 instead of 50-100.
#
# velocity_inference_scale controls correction magnitude at inference.
# dt * scale = 0.05 * 30 = 1.5 per frame, which is enough to shift
# a distribution toward the correct phoneme without overshooting.
# This can be tuned after training without retraining.
# ################################

seed: 1986
run_name: dfm_stage13_direction_decoupled
output_folder: !ref results/updated_ctc/<run_name>
__set_seed: !apply:speechbrain.utils.seed_everything [!ref <seed>]
test_wer_file: !ref <output_folder>/wer_test.txt
save_folder: !ref <output_folder>/save
train_log: !ref <output_folder>/train_log.txt

# ----------------------------------------------------------------------
# Data files
# ----------------------------------------------------------------------
data_folder: "E:/dfm_asr/data/TIMIT/data"
train_annotation: !ref <save_folder>/train.json
valid_annotation: !ref <save_folder>/dev.json
test_annotation: !ref <save_folder>/test.json
skip_prep: True
uppercase: True

# ----------------------------------------------------------------------
# Augmentation data
# ----------------------------------------------------------------------
data_folder_noise: !ref <data_folder>/noise
NOISE_DATASET_URL: https://www.dropbox.com/scl/fi/a09pj97s5ifan81dqhi4n/noises.zip?rlkey=j8b0n9kdjdr32o1f06t0cw5b7&dl=1
noise_annotation: !ref <save_folder>/noise.csv

# ----------------------------------------------------------------------
# Training parameters
# ----------------------------------------------------------------------
number_of_epochs: 50
batch_size: 16
lr: 1.0
sorting: ascending

# ----------------------------------------------------------------------
# Feature parameters
# ----------------------------------------------------------------------
sample_rate: 16000
n_fft: 400
n_mels: 40

# ----------------------------------------------------------------------
# Encoder model parameters (same as baseline)
# ----------------------------------------------------------------------
activation: !name:torch.nn.LeakyReLU
dropout: 0.15
cnn_blocks: 2
cnn_channels: (128, 256)
cnn_kernelsize: (3, 3)
rnn_layers: 4
rnn_neurons: 512
rnn_bidirectional: True
dnn_blocks: 2
dnn_neurons: 512

# ----------------------------------------------------------------------
# Output layer
# ----------------------------------------------------------------------
output_neurons: 40
blank_index: 0

# ----------------------------------------------------------------------
# DFM parameters
# ----------------------------------------------------------------------
dfm_epsilon: 1.0e-6
target_smoothing: 0.02
velocity_hidden: 512

# Euler integration steps: S=20 gives ~3 frames per window
num_flow_steps: 20

# Fixed magnitude for inference velocity normalisation
# correction = dt * scale = 0.05 * 30 = 1.5 per frame
# Tune this AFTER training by sweeping: 10, 15, 20, 25, 30, 40, 50
velocity_inference_scale: 3

# ----------------------------------------------------------------------
# Hybrid loss weights
# With unit-normalised targets, FM loss is ~0.5-2.0 (not ~80-100).
# lambda_fm: 1.0 gives comparable gradient contribution to CTC.
# velocity_weight_decay: L2 penalty on velocity net weights only.
# ----------------------------------------------------------------------
lambda_ctc: 1.0
lambda_fm: 1.0
velocity_weight_decay: 1.0e-5

# ----------------------------------------------------------------------
# Dataloader options
# ----------------------------------------------------------------------
num_workers: 0

train_dataloader_opts:
    batch_size: !ref <batch_size>
    num_workers: !ref <num_workers>

valid_dataloader_opts:
    batch_size: !ref <batch_size>
    num_workers: !ref <num_workers>

test_dataloader_opts:
    batch_size: !ref <batch_size>
    num_workers: !ref <num_workers>

# ----------------------------------------------------------------------
# Normalization
# ----------------------------------------------------------------------
normalize: !new:speechbrain.processing.features.InputNormalization
    norm_type: global

# ----------------------------------------------------------------------
# Augmentations
# ----------------------------------------------------------------------
prepare_noise_data: !name:speechbrain.augment.preparation.prepare_dataset_from_URL
    URL: !ref <NOISE_DATASET_URL>
    dest_folder: !ref <data_folder_noise>
    ext: wav
    csv_file: !ref <noise_annotation>

add_noise: !new:speechbrain.augment.time_domain.AddNoise
    csv_file: !ref <noise_annotation>
    snr_low: 5
    snr_high: 15
    noise_sample_rate: !ref <sample_rate>
    clean_sample_rate: !ref <sample_rate>
    num_workers: !ref <num_workers>

speed_perturb: !new:speechbrain.augment.time_domain.SpeedPerturb
    orig_freq: !ref <sample_rate>
    speeds: [98, 100, 102]

drop_freq: !new:speechbrain.augment.time_domain.DropFreq
    drop_freq_low: 0
    drop_freq_high: 1
    drop_freq_count_low: 1
    drop_freq_count_high: 2
    drop_freq_width: 0.03

drop_chunk: !new:speechbrain.augment.time_domain.DropChunk
    drop_length_low: 400
    drop_length_high: 1000
    drop_count_low: 1
    drop_count_high: 2

wav_augment: !new:speechbrain.augment.augmenter.Augmenter
    concat_original: False
    min_augmentations: 1
    max_augmentations: 2
    augment_prob: 0.5
    augmentations: [
        !ref <add_noise>,
        !ref <speed_perturb>,
        !ref <drop_freq>,
        !ref <drop_chunk>
    ]

# ----------------------------------------------------------------------
# Training helpers
# ----------------------------------------------------------------------
epoch_counter: !new:speechbrain.utils.epoch_loop.EpochCounter
    limit: !ref <number_of_epochs>

compute_features: !new:speechbrain.lobes.features.Fbank
    sample_rate: !ref <sample_rate>
    n_fft: !ref <n_fft>
    n_mels: !ref <n_mels>

# ----------------------------------------------------------------------
# Encoder (CRDNN acoustic model)
# ----------------------------------------------------------------------
model: !new:speechbrain.lobes.models.CRDNN.CRDNN
    input_size: !ref <n_mels>
    activation: !ref <activation>
    dropout: !ref <dropout>
    cnn_blocks: !ref <cnn_blocks>
    cnn_channels: !ref <cnn_channels>
    cnn_kernelsize: !ref <cnn_kernelsize>
    time_pooling: True
    rnn_layers: !ref <rnn_layers>
    rnn_neurons: !ref <rnn_neurons>
    rnn_bidirectional: !ref <rnn_bidirectional>
    dnn_blocks: !ref <dnn_blocks>
    dnn_neurons: !ref <dnn_neurons>

# Linear projection head: H -> Z (logits)
output: !new:speechbrain.nnet.linear.Linear
    input_size: !ref <dnn_neurons>
    n_neurons: !ref <output_neurons>
    bias: True

# Velocity network: direction-only prediction
# 512 hidden, 3 conv layers, 0.1 dropout, GroupNorm
velocity_net: !new:__main__.VelocityNet
    vocab_size: !ref <output_neurons>
    hidden_size: !ref <dnn_neurons>
    velocity_hidden: !ref <velocity_hidden>

log_softmax: !new:speechbrain.nnet.activations.Softmax
    apply_log: True

# ----------------------------------------------------------------------
# Optimizer / scheduler
# ----------------------------------------------------------------------
opt_class: !name:torch.optim.Adadelta
    rho: 0.95
    lr: !ref <lr>
    eps: 1.e-8

lr_annealing: !new:speechbrain.nnet.schedulers.NewBobScheduler
    initial_value: !ref <lr>
    improvement_threshold: 0.0025
    annealing_factor: 0.8
    patient: 0

# ----------------------------------------------------------------------
# Registered trainable modules
# ----------------------------------------------------------------------
modules:
    model: !ref <model>
    output: !ref <output>
    velocity_net: !ref <velocity_net>
    normalize: !ref <normalize>

jit_module_keys: [model]

# ----------------------------------------------------------------------
# Checkpointing
# ----------------------------------------------------------------------
checkpointer: !new:speechbrain.utils.checkpoints.Checkpointer
    checkpoints_dir: !ref <save_folder>
    recoverables:
        model: !ref <model>
        output: !ref <output>
        velocity_net: !ref <velocity_net>
        scheduler: !ref <lr_annealing>
        normalizer: !ref <normalize>
        counter: !ref <epoch_counter>

# ----------------------------------------------------------------------
# Loss / logging / metrics
# ----------------------------------------------------------------------
compute_cost: !name:speechbrain.nnet.losses.ctc_loss
    blank_index: !ref <blank_index>

train_logger: !new:speechbrain.utils.train_logger.FileTrainLogger
    save_file: !ref <train_log>

ctc_stats: !name:speechbrain.utils.metric_stats.MetricStats
    metric: !name:speechbrain.nnet.losses.ctc_loss
        blank_index: !ref <blank_index>
        reduction: batch

per_stats: !name:speechbrain.utils.metric_stats.ErrorRateStats